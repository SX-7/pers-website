---
layout:
eleventyNavigation:
  hide: true
eleventyExcludeFromCollections: true
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0"
    />
    <title>Content Manager</title>
    <link
      href="config.yml"
      type="text/yaml"
      rel="cms-config-url"
    />
  </head>
  <body>
    <script src="https://unpkg.com/decap-cms@^3.0.0/dist/decap-cms.js"></script>
    <script
      id="bundle-css-source"
      type="text/plain"
    >
      {% include 'bundle-css.njk' %}
    </script>
    <script>
      const bundledCSS =
        document.getElementById("bundle-css-source").textContent;
      CMS.registerPreviewStyle(bundledCSS, { raw: true });

      const PostPreview = createClass({
        render: function () {
          const entry = this.props.entry;
          const body = this.props.widgetFor("body");

          return h(
            "div",
            {
              className: "bg-slate-900 min-h-screen p-8",
            },
            h(
              "div",
              {
                className: "prose lg:prose-xl prose-invert prose-slate mx-auto",
              },
              body,
            ),
          );
        },
      });

      CMS.registerPreviewTemplate("projects", PostPreview);
    </script>
  </body>
</html>
